# CodeView Environment Variables
# No API keys required — CodeView uses your existing Claude Code installation

# Set by the CLI automatically — you don't need to configure this
# CODEVIEW_PROJECT_DIR=/path/to/your/project
